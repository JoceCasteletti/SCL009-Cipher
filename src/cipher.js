window.cipher = {
  encode: (ofsset,text) => {
    let encode = '';
    /* Acá va tu código */
    for(let i=0; i=<text.length,i++){
  }
    return result;
  },



  decode: (message,ofset) => {
    /* Acá va tu código */
  }
};
